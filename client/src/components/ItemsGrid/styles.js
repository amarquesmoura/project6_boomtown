const styles = theme => ({
  media: {
    height: 0,
    paddingTop: '56.25%'
  },
  itemTitle: {
    fontWeight: 600
  },
  borrow: {
    marginBottom: theme.spacing.unit * 2
  }
});

export default styles;
